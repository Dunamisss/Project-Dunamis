rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isAdmin() {
      return isSignedIn() && (
        request.auth.token.admin == true ||
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.accessTier == "admin"
      );
    }

    function userSecurityFieldsValidOnCreate(userId) {
      return request.resource.data.uid == userId
        && request.resource.data.accessTier == "free"
        && request.resource.data.banned == false
        && request.resource.data.strikes == 0;
    }

    function userSecurityFieldsUnchangedOnUpdate() {
      return request.resource.data.uid == resource.data.uid
        && request.resource.data.accessTier == resource.data.accessTier
        && request.resource.data.banned == resource.data.banned
        && request.resource.data.strikes == resource.data.strikes;
    }

    // Users can read own profile; admins can read all profiles
    match /users/{userId} {
      allow read: if isOwner(userId) || isAdmin();
      allow create: if isOwner(userId) && userSecurityFieldsValidOnCreate(userId);
      allow update: if (isOwner(userId) && userSecurityFieldsUnchangedOnUpdate()) || isAdmin();
      allow delete: if isAdmin();
    }

    // Private prompt packs per user profile
    match /users/{userId}/promptPacks/{packId} {
      allow read, write: if isOwner(userId);
    }

    // Private optimizer history per user profile
    match /users/{userId}/optimizerHistory/{itemId} {
      allow read, write: if isOwner(userId);
    }

    // Private song drafts per user profile
    match /users/{userId}/songDrafts/{draftId} {
      allow read, write: if isOwner(userId);
    }

    // Private song usage analytics per user profile
    match /users/{userId}/songUsageEvents/{eventId} {
      allow read, write: if isOwner(userId);
    }

    // Unified community submissions flow
    match /submissions/{submissionId} {
      allow create: if isSignedIn()
        && request.resource.data.userId == request.auth.uid
        && (request.resource.data.type == "prompt" || request.resource.data.type == "image")
        && request.resource.data.status == "pending";

      allow read: if isAdmin()
        || resource.data.status == "approved"
        || (isSignedIn() && resource.data.userId == request.auth.uid);

      allow update, delete: if isAdmin();
    }

    // Prompt submissions are moderatable; authenticated users can create
    // Only admins may create submissions marked as 'bulk' (server/admin uploads)
    // LOCKED prompts only readable by admins
    match /promptSubmissions/{submissionId} {
      allow create: if isSignedIn()
        && request.resource.data.userId == request.auth.uid
        && (
          // If no submissionSource or it's 'form', allow for any authenticated user
          !(request.resource.data.submissionSource == 'bulk')
          || isAdmin()
        );
      allow read: if 
        // Admins can always read
        isAdmin()
        || (
          // Non-admins can read only if not locked
          !resource.data.isLocked
        );
      allow update, delete: if isAdmin();
    }

    // Default deny
    match /{document=**} {
      allow read: if false;
      allow write: if false;
    }
  }
}
